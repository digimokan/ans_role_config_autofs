# the collection of autofs services that must be started and enabled
autofs_linux_service_names:
    - "autofs"
autofs_freebsd_service_names:
    - "automount"
    - "automountd"
    - "autounmountd"

